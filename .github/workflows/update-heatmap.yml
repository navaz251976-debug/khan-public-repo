name: Update Heatmap

on:
  schedule:
    - cron: '*/15 11-23 * * 1-5'   # every 15 mins, 6AM–6PM ET (11:00–23:00 UTC) Mon–Fri
  workflow_dispatch:            # manual trigger button in GitHub UI

permissions:
  contents: write
  pages: write
  id-token: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Restore prices cache
        uses: actions/cache@v4
        with:
          path: prices_cache.parquet
          key: prices-${{ github.run_id }}
          restore-keys: prices-

      - name: Install dependencies
        run: pip install -r requirements.txt tzdata

      - name: Generate heatmap and charts
        run: python heatmap.py

      - name: Commit and push
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/index.html
          git diff --staged --quiet || git commit -m "chore: update heatmap $(date -u +'%Y-%m-%d %H:%M UTC')"
          git pull --rebase --autostash origin main
          git push

      - name: Setup Pages
        uses: actions/configure-pages@v5

      # Charts in docs/charts/ are generated fresh each run and gitignored —
      # they reach the site only through this artifact, so the repo never grows.
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'docs'

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
